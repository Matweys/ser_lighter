# 🎮 Режим симуляции торговли

## ✅ Что изменилось

### 1. **Убрана зависимость от реальных API ключей Lighter**
   - ❌ `LIGHTER_API_PRIV` - больше не нужен
   - ❌ `LIGHTER_ACCOUNT_INDEX` - больше не нужен
   - ✅ Только публичные данные для получения цен

### 2. **PostgreSQL заменен на SQLite**
   - ✅ Файл `lighter_trading.db` в корне проекта
   - ✅ Не требует установки PostgreSQL
   - ✅ Легко экспортировать и анализировать

### 3. **Полная симуляция торговли**
   - 🎮 Все сделки виртуальные
   - 💰 Реальные деньги НЕ используются
   - 📊 Цены реальные (публичные данные Lighter)
   - 💸 Комиссии симулируются (0.055% как у Bybit)

## 🚀 Запуск

### Минимальная настройка в `.env`:

```bash
# Telegram (обязательно)
TELEGRAM_TOKEN=8327834182:AAEMAZw1NDSIEf6jbB-VU9CDTNv8FSxLgAo
TELEGRAM_CHANNEL_ID=-1003110580787

# Redis (для конфигураций)
REDIS_URL=redis://localhost:6379/0

# Lighter (опционально, только символ)
LIGHTER_SYMBOL=SOL
```

### Запуск:

```bash
python lighter_trading_bot.py
```

## 📊 Что работает

✅ **Реальные данные:**
- Цены (публичные данные Lighter)
- Исторические свечи (для анализа EMA+RSI)

🎮 **Симуляция:**
- Размещение ордеров (виртуальные)
- Исполнение ордеров (мгновенное)
- Позиции (в памяти)
- Комиссии (0.055% от суммы)

## 💾 База данных

Все сделки сохраняются в SQLite файл `lighter_trading.db`:

```sql
-- Просмотр всех сделок
SELECT * FROM trades ORDER BY created_at DESC;

-- Статистика
SELECT 
    COUNT(*) as total_trades,
    SUM(CASE WHEN profit > 0 THEN 1 ELSE 0 END) as winning_trades,
    SUM(profit) as total_profit
FROM trades
WHERE status = 'CLOSED';
```

## 📱 Telegram уведомления

Все уведомления идут в канал:
- ✅ Вход в позицию
- 💰 Усреднение
- ✅ Выход с результатом

## 🔍 Проверка работы

После запуска проверьте:
1. ✅ Файл `lighter_trading.db` создан
2. ✅ Уведомления приходят в Telegram канал
3. ✅ Логи показывают симуляцию сделок

## ⚙️ Настройка комиссий

Комиссии симулируются как 0.055% (taker fee Bybit).
Чтобы изменить, отредактируйте `api/lighter_simulator.py`:

```python
commission_rate = Decimal('0.00055')  # Измените на нужное значение
```

